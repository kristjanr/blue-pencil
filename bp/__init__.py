"""Blue Pencil — an editorial pipeline for continuing an unfinished long series.

The engine knows nothing about any particular series. Everything series-specific
lives in a profile (see :mod:`bp.profile`); everything run-specific lives in a run
policy (see :mod:`bp.policy`).
"""

__version__ = "0.3.0"
