# canonical name: [every alias the text uses]
# Retrieval that misses a scene because the text says "Will" and the graph says
# "Riker" is the most common silent failure in this design, so spell them out.
